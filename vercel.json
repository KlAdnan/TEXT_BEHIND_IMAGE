
{
  "version": 2,                         // Vercel config version
  "builds": [{
    "src": "package.json",             // Source file for build
    "use": "@vercel/next"             // Use Next.js builder
  }],
  "env": {
    "PORT": "8000"                    // Set application port
  }
}
